import React from "react";
import ReactDOM from "react-dom";

import AppIntl from "./components/App/AppIntl";

ReactDOM.render(
  <React.StrictMode>
    <AppIntl />
  </React.StrictMode>,
  document.getElementById("root")
);
